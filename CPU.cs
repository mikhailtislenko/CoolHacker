namespace CoolHacker
{
    class CPU : Part
    {
        public CPU(string Name, string PCType, string Socket, string RamType, string RamCapasity, string VCInterface, string DiscInterface) : base(Name, PCType, Socket, RamType, RamCapasity, VCInterface, DiscInterface)
        {

        }
    }
}
